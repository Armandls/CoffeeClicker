package Presentation.Controllers;

/**
 * Interface for managing store currency updates.
 */
public interface ThreadController {
    /**
     * Updates the store currency with the specified amount.
     *
     * @param amount The amount to update the store currency.
     */
    void updateStoreCurrency(float amount);
}
